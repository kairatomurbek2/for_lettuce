"""
This package contains modules that implement navigations.

The modules implement functions to navigate to other parts
of the system.

Note:
	Each module corresponds to only one navigation menu.
	If there is another navigation menu, use module that
	corresponds to it.

Names of modules follow the template
`navigation_name + _navigator.py`.
"""